import { NextBabelLoaderOptions, NextJsLoaderContext } from './types';
export default function getConfig(this: NextJsLoaderContext, { source, loaderOptions, inputSourceMap, target, filename, }: {
    source: string;
    loaderOptions: NextBabelLoaderOptions;
    inputSourceMap?: object | null;
    target: string;
    filename: string;
}): any;
